# タスク：取り決めは「入力」で作る

- 対応する設計：`design.md` / `design-upcoming.md`
- 締切：2026-08-15 15:00
- 方針：**壊さない順に進める。**消さずに、呼び出しを外す

---

## 進め方

| 順 | まとまり | なぜこの順か |
| --- | --- | --- |
| 1 | 対話から切り離す（T1・T2） | 小さく、方針の核。**ここが通れば後戻りしない** |
| 2 | 取り決め画面（T3〜T6） | 最大の作業。デモの中心 |
| 3 | オンボーディング（T7） | 上が動いてから削る |
| 4 | 決まったこと（T8） | 表示を絞るだけ |
| 5 | シナリオ（T9） | 価値の言語化。データ追加のみ |
| 6 | デモ・説明資料（T10） | **提出物そのもの** |

---

## T1. 対話から取り決めへの経路を断つ

- [ ] `relayIfNeeded` から `appendProposal` を外す（Adjustment だけ残す）
- [ ] `effectQuestion`（今回だけ／今後も）を対話で問わない
- [ ] テスト：**対話からは提案が作られない**
- [ ] E2E：⑥合意形成を「ウィザードで合意する」に書き換える

**完了条件**：対話を何回続けても `proposals` が増えない

## T2. 算定表は範囲だけにする

- [ ] `buildMediationDraft` の LARGE 呼び出しを外す（範囲の提示だけ残す）
- [ ] 調停案のキャッシュを使わない
- [ ] テスト：**説明文が出ない**
- [ ] `docs/` の CT-1 記述を更新（原価が下がる）

**完了条件**：AI が金額に触れる経路が無い

## T3. 条項の `condition` を実装する

- [ ] `buildDocument` で `condition: { field, equals }` を評価
- [ ] **条件に使う値が payload に無ければ、その条項を出さない**
- [ ] テスト（G-3 に追加）

**完了条件**：財産分与の3通りが出し分けられる

## T4. 財産分与・年金分割を足す

- [ ] `ps_property_division_v1` ＋ 条項3本（一括／清算済み／別途協議）
- [ ] `ps_pension_split_v1` ＋ 条項1本
- [ ] `IMPLEMENTED_TOPICS` に追加
- [ ] **★ 年金分割：按分割合は用意した選択肢だけ。**任意の数値を受け付けない
- [ ] **★ 一次資料の確認**（按分割合の上限・情報通知書）。
      確認できるまで**未検証の注記を外さない**

**完了条件**：4論点すべてに条項ひな形があり、G-3 が通る

## T5. 取り決めの入力・編集画面

- [ ] `/app/agreements` を4論点の一覧にする（状態を1行で）
- [ ] `/app/agreements/[topic]` を作る（未入力／下書き／返事待ち／別案／合意済／取り下げ）
- [ ] `terms` API を4論点に開く
- [ ] 合意済のときは N-1 ＋「変更を申し出る」（K-6）

**完了条件**：**対話を一度も使わずに4論点を取り決めにできる**（AC-1）

## T5b. ★ 仮案と共有の切り替え（`design-sharing.md`）

- [ ] `Proposal` に `sharedAt` / `withdrawnAt`
- [ ] **★ `sharedAt` の無い提案を、相手側の API が返さない**（画面で隠さない）
- [ ] `consentStateOf` に「下書き」「取り下げ」
- [ ] 了承は**相手の提案をそのまま承諾する**（自分の payload を作らない）
- [ ] 渡したとき相手に届く（状態変化だけで終わらせない）
- [ ] テスト：**下書きは相手から見えない**／取り下げは相手に見える
- [ ] テスト：了承の経路では `payloadsAgree` が自明に真

**完了条件**：**渡すまで相手に見えない。**渡したことも取り下げたことも相手に届く

## T6. 必要な情報を、必要になった時点で聞く

- [ ] 養育費の入力で、お子さん・同居・年収が欠けていればその場で伺う
- [ ] 既存の画面（`children` / `living` / `profile`）を再利用する
- [ ] **飛ばせる。**飛ばすと範囲が出ないだけ
- [ ] 「目安が出ないと決められません」と書かない

**完了条件**：オンボーディングを通らずに算定表まで到達できる

## T7. オンボーディングを3枚にする

- [ ] `/`（説明）→ `/account`（メール登録）→ `/onboarding/invite`（招待するか）→ `/app`
- [ ] `/start`（状況の5択）を通り道から外す
- [ ] 招待は「いま渡す／あとにする」を**同じ重さ**で
- [ ] 「お渡しになるまで、お相手には何も届きません」を置く
- [ ] 関係の状態を**一切聞かない**

**完了条件**：新規の人が3枚で `/app` に着く

## T8. 「決まったこと」にする

- [ ] タブ名を **「これから」→「決まったこと」**
- [ ] 予定・履行・逸脱・先取特権・リマインドの表示を外す
- [ ] 約束・今回だけの変更・揃った調整だけにする
- [ ] L-3 を書き換え、**「通知は設定で切れます」を外す**
- [ ] E2E：履行・逸脱の検査を外す

**完了条件**：スケジュール管理に見えない

## T9. シナリオを足す

- [ ] 「会えなかったことを相談する」（子どもと会うこと・ADJUSTMENT）
- [ ] 「お支払いのことで相談する」（お金のこと・ADJUSTMENT）
- [ ] 書き出しの案内。**責める言い方を例に置かない**

**完了条件**：うまくいかなかったときの入口がある

## T10. デモと説明資料

- [ ] デモの筋書き（★C1 → 取り決めのウィザード → 公正証書の原案 → 相談の仲介）
- [ ] 検証用リンクを作り直す
- [ ] 説明資料（価値仮説：目的抽出・感情フィルタリング・仲介）

**完了条件**：**提出できる**

---

## 落とさないもの（各タスク共通）

- [ ] テストが通る（いま 710 件）／型・Lint エラー 0
- [ ] E2E が通る
- [ ] **C1**：原文が相手に渡らない
- [ ] **P3**：数字と条項を LLM に作らせない（今回、例外が無くなる）
- [ ] 実名を公開文書に書かない

## 締切に間に合わないときに削る順

1. T9（シナリオ）… データ追加なので最後でよい
2. T4 の年金分割 … **一次資料が確認できなければ出さない**
3. T7（オンボーディング）… いまの形でもデモは通る

**T1・T2・T5・T5b は削らない。**方針そのものだから。

---

## 今回やらないと決めたもの

| | 行き先 |
| --- | --- |
| **公正証書そのもの（PDF・画像）を預かる** | **Issue #6 に追記。**置き場所だけデザインに入れる（`design-sharing.md` §8） |
