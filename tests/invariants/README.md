# 不変条件テスト

**これらは機能テストではない。プロダクトが成立する条件の検証である。**

落ちたらリリースしない。

| # | 不変条件 |
| --- | --- |
| INV-1 | `Message.content` は、`partyId` が一致するセッション以外のLLMコンテキストに含まれない |
| INV-2 | `ContactInfo` の各フィールドは、いかなるLLMコンテキストにも含まれない |
| INV-2a | 精密な年収は越えない。越えるのは `incomeBand` のみ |
| INV-3 | 当事者間を越えられるのは5つのみ |
| INV-4a | 越境テキストは、原文とN文字以上の連続一致を持たない |

## ★ このテストは実装より先に書かれた

実装を見ながらテストを書くと、実装が見落とした経路はテストでも見落とされる。
INV でこれをやると、C1 が破れていても気づけない。

→ docs/development-guidelines.md §5.0
